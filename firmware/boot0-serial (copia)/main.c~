/**
 * Primitive first stage bootloader 
 *
 *
 */
#include "soc-hw.h"

int main()
{
	uint32_t x=1;
	uint8_t  c;

	// Initialize UART

	c='u';
uart_putchar('i');

gpio0->dir=0x0F;

//for(;;)
prueba1();
	
  /*for(;;)
{
  uart_putchar(uart_getchar1());

}*/
	
}
